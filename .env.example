# RowFantasy Environment Configuration
# Copy this file to .env and fill in actual values
# NEVER commit .env with real secrets to version control

# Supabase Configuration (provided by Lovable Cloud)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# Backend Secrets (set in Lovable Cloud → Secrets)
# These are already configured for you, this file is for documentation only
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Payment Provider Configuration (optional - defaults to mock)
# Uncomment and set in Lovable Cloud → Secrets for production
# PAYMENTS_PROVIDER=mock
# HIGHRISK_API_KEY=your-highrisk-key
# HIGHRISK_WEBHOOK_SECRET=your-webhook-secret
# ACH_PARTNER_ID=your-ach-partner-id
# ACH_WEBHOOK_SECRET=your-ach-webhook-secret

# Feature Flags (configured in database, not env vars)
# See feature_flags table for runtime configuration
# real_money_enabled: Controls whether real money transactions are allowed

# Node Environment
NODE_ENV=development

# App Configuration
# VITE_APP_NAME=RowFantasy
# VITE_APP_URL=https://your-app.lovable.app
